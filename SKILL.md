---
name: ai-novel-agent
description: AI 长篇小说（网文）写作智能体系统：三人格协作流水线（选品 Scout / 写作 Author / 评审 Reader）＋跨会话状态块＋闸门质检＋去 AI 味规则台账＋AI 生成内容声明合规。当用户要写小说/网文、搭小说生产流程、做选品定题材、写细纲或正文、评审章节、跨会话续写不丢进度、去 AI 味、或需要 AI 生成内容声明时使用。
---

# AI 小说智能体

把"AI 写长篇"从单次对话升级为**跨会话、可质检、可追溯**的生产系统。核心不是提示词，是「状态＋契约＋闸门」：

- **状态**：一个 11 行的状态块，纯对话环境可粘贴携带，换会话不丢进度
- **契约**：H1–H6 交接物，每步的输入/输出/验收有唯一定义
- **闸门**：G1/G2/G3 三道关，三态记录（pending/passed/waived），防"自我宣布通过"

## 目录结构（本包内相对路径，引用时保持原样）

```
10_内核/          ← 状态、闸门、交接物、目录模板、日志、声明（系统的"法律"）
20_人格/          ← Scout/Author/Reader 三张人格卡（可单独粘贴到任意聊天工具）
30_台账/          ← 去 AI 味规则台账（空表＋规格）
tools/check-bundle.py  ← 交付集自洽校验器
```

## 工作流（按用户所处阶段路由）

### 阶段 0：建项目

1. 读 `10_内核/05_项目目录模板.md`，按其中顺序创建书的目录骨架
2. 用 `10_内核/01_项目卡模板.md` 建项目卡（稳定元信息：题材/篇幅/文风锚/硬约束）
3. 从 `10_内核/02_状态规格.md` 生成初始状态块，交给用户保存

### 阶段 1：选品（用户没有确定题材时）

读 `20_人格/10_选品人格_Scout.md` 并按其执行。**铁律：禁止凭训练记忆编造榜单/数据**；没有真实数据时输出"数据缺失，无法给出选题"，不编。

### 阶段 2：细纲与正文

1. 读 `20_人格/20_作家人格_Author.md` 并按其执行
2. 每章开始前**先向用户索要状态块**，按块内第一行指令校验：任一必需键缺失 → 停下并指出缺了哪个键，**不要静默沿用旧值**
3. 细纲缺「核心冲突」「章末钩子」任一项 → **不写**；缺「爽点」→ 可写但平写、**禁止自行发明**
4. **不得引入细纲之外的设定**（人名/金手指/世界观规则）；需要新设定时停下来问用户
5. 正文完成后按 `10_内核/08_产出物声明模板.md` **立即**写声明字段（不许事后凭记忆补填；记不清一律填 `AI生成`）

### 阶段 3：评审

1. 读 `20_人格/30_读者人格_Reader.md` 并按其执行：只读正文、引用原句、强制定位最弱段落
2. 读 `20_人格/00_通用独立性规则.md`：**同一模型既写又评＝弱审稿**，须向用户声明独立性等级；条件允许时建议用户换一个模型做读者

### 阶段 4：闸门与记账

1. 按 `10_内核/03_闸门规则.md` 处理 G1/G2/G3；`waived ≠ passed`，不得混记
2. 状态变更按 `10_内核/02_状态规格.md` 的字段归属更新（"每个字段只有一个家"：稳定元信息归项目卡，易变状态归状态块）
3. 动作记入 `10_内核/06_动作日志模板.md` 格式的 append-only 日志
4. 去逐味规则按 `30_台账/00_台账规格.md` 记录；台账为空时**必须向用户报警**（这是预期行为，不是故障）

## 不可违反的硬约束

1. **检测器只报位置，不改文**——发现 AI 味时指出句子和原因，重写由人决定
2. **细纲停笔红线只有 2 项**（核心冲突/章末钩子）；其余缺项"可写不可编"
3. **产出物声明"缺省即最严"**：不确定就标 AI 生成；系统只产出声明文本，不替用户提交平台
4. **手改标不产生检定加成**：用户手动改过的段落，不因此放宽闸门
5. 状态块校验失败时**停下报告**，禁止静默继续

## 自检

对本 skill 目录做结构性修改后，运行 `python tools/check-bundle.py`（在 skill 根目录下执行）。退出码 0 且无 FAIL/WARN 才算改完。它检查：文件引用、章节引用、表格列数、围栏配对、占位符、关键数字。

## 环境适配

- 本包不含语料盘点工具（那些脚本依赖用户本机的书籍语料路径）
- 纯对话环境（无文件系统）下：状态块是唯一载体，每轮让用户粘贴；人格卡可整段作为提示词使用
- 有文件系统时：状态块落盘为 `99_状态/state.json`，字段名与状态块逐字一致
